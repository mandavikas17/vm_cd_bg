apiVersion: v1
kind: Service
metadata:
  name: webapp-primary
  labels:
    app: webapp
spec:
  type: ClusterIP
  selector:
    app: webapp
    harness.io/color: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
